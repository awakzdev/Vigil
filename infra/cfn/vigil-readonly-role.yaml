AWSTemplateFormatVersion: "2010-09-09"
Description: >
  Vigil read-only IAM role for compliance scanning. Enumerates AWS config
  across IAM, storage, compute, networking, and security services. No write
  access. Role retained on stack deletion.

Parameters:
  VigilAccountPrincipal:
    Type: String
    Default: "arn:aws:iam::016266969060:role/VigilControlPlane"
    Description: ARN of the Vigil control plane principal (provided during onboarding).
  ExternalId:
    Type: String
    Description: Unique ExternalId issued at onboarding. Required to assume the role.
    NoEcho: true
  RoleName:
    Type: String
    Default: VigilReadOnly
    Description: IAM role name. Do not change on stack updates.

Resources:
  VigilRole:
    Type: AWS::IAM::Role
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      RoleName: !Ref RoleName
      Description: Vigil read-only compliance scanner. No write access.
      MaxSessionDuration: 3600
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS: !Ref VigilAccountPrincipal
            Action: sts:AssumeRole
            Condition:
              StringEquals:
                sts:ExternalId: !Ref ExternalId
      Policies:
        - PolicyName: VigilMinimalReadOnly
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: IamUserAndKeyEnumeration
                Effect: Allow
                Action:
                  - iam:ListUsers
                  - iam:ListMFADevices
                  - iam:GetLoginProfile
                  - iam:ListAccessKeys
                  - iam:GetAccessKeyLastUsed
                  - iam:GetAccountSummary
                  - iam:ListAccountAliases
                  - iam:GetAccountPasswordPolicy
                Resource: "*"

              - Sid: IamRoleEnumeration
                Effect: Allow
                Action:
                  - iam:ListRoles
                  - iam:ListRolePolicies
                  - iam:GetRolePolicy
                  - iam:ListAttachedRolePolicies
                  - iam:GetPolicy
                  - iam:GetPolicyVersion
                  - iam:ListPolicies
                Resource: "*"

              - Sid: IamServiceLastAccessed
                Effect: Allow
                Action:
                  - iam:GenerateServiceLastAccessedDetails
                  - iam:GetServiceLastAccessedDetails
                Resource: "*"

              - Sid: S3BucketConfiguration
                Effect: Allow
                Action:
                  - s3:ListAllMyBuckets
                  - s3:GetAccountPublicAccessBlock
                  - s3:GetBucketLogging
                  - s3:GetEncryptionConfiguration
                  - s3:GetBucketVersioning
                  - s3:GetBucketPublicAccessBlock
                  - s3:GetBucketPolicy
                  - s3:GetBucketAcl
                Resource: "*"

              - Sid: KmsKeyConfiguration
                Effect: Allow
                Action:
                  - kms:ListKeys
                  - kms:DescribeKey
                  - kms:GetKeyRotationStatus
                  - kms:GetKeyPolicy
                  - kms:ListAliases
                Resource: "*"

              - Sid: CloudTrailConfiguration
                Effect: Allow
                Action:
                  - cloudtrail:DescribeTrails
                  - cloudtrail:GetTrailStatus
                  - cloudtrail:LookupEvents
                Resource: "*"

              - Sid: GuardDutyConfiguration
                Effect: Allow
                Action:
                  - guardduty:ListDetectors
                  - guardduty:GetDetector
                  - guardduty:ListFindings
                  - guardduty:GetFindings
                Resource: "*"

              - Sid: SecurityHubConfiguration
                Effect: Allow
                Action:
                  - securityhub:DescribeHub
                Resource: "*"

              - Sid: VpcAndSecurityGroupEnumeration
                Effect: Allow
                Action:
                  - ec2:DescribeRegions
                  - ec2:DescribeVpcs
                  - ec2:DescribeFlowLogs
                  - ec2:DescribeSecurityGroups
                Resource: "*"

              - Sid: Ec2InstanceEnumeration
                Effect: Allow
                Action:
                  - ec2:DescribeInstances
                  - ec2:DescribeVolumes
                  - ec2:DescribeSnapshots
                  - ec2:DescribeSnapshotAttribute
                  - ec2:DescribeImages
                  - ec2:GetEbsEncryptionByDefault
                Resource: "*"

              - Sid: AccessAnalyzerConfiguration
                Effect: Allow
                Action:
                  - access-analyzer:ListAnalyzers
                Resource: "*"

              - Sid: ConfigServiceConfiguration
                Effect: Allow
                Action:
                  - config:DescribeConfigurationRecorders
                  - config:DescribeConfigurationRecorderStatus
                  - config:DescribeDeliveryChannels
                  - config:DescribeComplianceByConfigRule
                Resource: "*"

              - Sid: IdentityCenterDirectory
                Effect: Allow
                Action:
                  - sso:ListInstances
                  - sso:DescribeInstance
                  - identitystore:ListUsers
                  - identitystore:DescribeUser
                Resource: "*"

              - Sid: RdsConfiguration
                Effect: Allow
                Action:
                  - rds:DescribeDBInstances
                Resource: "*"

              - Sid: AcmCertificates
                Effect: Allow
                Action:
                  - acm:ListCertificates
                  - acm:DescribeCertificate
                Resource: "*"

              - Sid: LambdaConfiguration
                Effect: Allow
                Action:
                  - lambda:ListFunctions
                  - lambda:GetFunctionEventInvokeConfig
                Resource: "*"

              - Sid: SecretsManagerConfiguration
                Effect: Allow
                Action:
                  - secretsmanager:ListSecrets
                Resource: "*"

              - Sid: SsmParameters
                Effect: Allow
                Action:
                  - ssm:DescribeParameters
                Resource: "*"

              - Sid: ElbConfiguration
                Effect: Allow
                Action:
                  - elasticloadbalancing:DescribeLoadBalancers
                  - elasticloadbalancing:DescribeLoadBalancerAttributes
                  - elasticloadbalancing:DescribeListeners
                Resource: "*"

              - Sid: DynamoDbConfiguration
                Effect: Allow
                Action:
                  - dynamodb:ListTables
                  - dynamodb:DescribeTable
                  - dynamodb:DescribeContinuousBackups
                Resource: "*"

              - Sid: SnsConfiguration
                Effect: Allow
                Action:
                  - sns:ListTopics
                  - sns:GetTopicAttributes
                Resource: "*"

              - Sid: SqsConfiguration
                Effect: Allow
                Action:
                  - sqs:ListQueues
                  - sqs:GetQueueAttributes
                Resource: "*"

              - Sid: OrganizationsAccountLabel
                Effect: Allow
                Action:
                  - organizations:DescribeAccount
                Resource: "*"

Outputs:
  RoleArn:
    Description: Paste this ARN into Vigil to finish onboarding.
    Value: !GetAtt VigilRole.Arn
  ExternalId:
    Description: ExternalId required to assume the role.
    Value: !Ref ExternalId
